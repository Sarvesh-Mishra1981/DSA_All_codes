 #include<iostream>
 using namespace std;
 
// for (int i = 0; i < n; i++) {
//             long long shift = a[n-i-1] % 26; 
//             b[i] = char('a' + (s[i] - 'a' + shift) % 26);
//         }

        //if u wnat soemthing like any num should shift like a+1=b like thta so problem is that z ,, how will u convert it into a . so this 
        //is the logic for that just do %26 to num by which u need to shift and then -'a' then do %26 and in the end just add a